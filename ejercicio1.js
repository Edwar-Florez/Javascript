// 1. Leer un número, imprimirlo junto con su positivo si es negativo.
function imprimirNegativo(numero) {
    if (numero < 0) {
        console.log(`Número: ${numero}, Positivo: ${-numero}`);
    }
}