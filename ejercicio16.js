// 16. Capturar un número y decir si es par o impar.
function esParOImpar(numero) {
    if (numero % 2 === 0) {
        console.log(`${numero} es par`);
    } else {
        console.log(`${numero} es impar`);
    }
}